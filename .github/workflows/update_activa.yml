name: Actualizar Activa

on:
  schedule:
    - cron: '0 0 * * *'

jobs:
  update-activa:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3
      - name: Install deps
        run: |
          & 'C:\Program Files\Python313\python.exe' -m pip install requests beautifulsoup4
      - name: Run update_activa
        env:
          PAT: ${{ secrets.PAT }}
        run: |
          & 'C:\Program Files\Python313\python.exe' update_activa.py
